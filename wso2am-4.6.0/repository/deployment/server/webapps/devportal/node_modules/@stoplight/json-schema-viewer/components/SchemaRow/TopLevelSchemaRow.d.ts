import * as React from 'react';
import { SchemaRowProps } from './SchemaRow';
export declare const TopLevelSchemaRow: ({ schemaNode, skipDescription, }: Pick<SchemaRowProps, 'schemaNode'> & {
    skipDescription?: boolean;
}) => React.JSX.Element;
